import styled from 'styled-components';

const H2 = styled.h2`
  font-size: 10vw;
  text-transform: lowercase;
  margin: 0;
  letter-spacing: -0.35vw;
  position: relative;
  overflow: hidden;
`;

export default H2;
