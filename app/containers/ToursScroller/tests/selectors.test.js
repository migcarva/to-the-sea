// import { selectToursScrollerDomain } from '../selectors';

describe('selectToursScrollerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
