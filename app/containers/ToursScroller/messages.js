/*
 * ToursScroller Messages
 *
 * This contains all the text for the ToursScroller container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ToursScroller';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ToursScroller container!',
  },
});
